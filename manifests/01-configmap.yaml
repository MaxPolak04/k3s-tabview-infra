apiVersion: v1
kind: ConfigMap
metadata:
  name: tabview-config
  namespace: prod
  labels:
    app: tabview
data:
  DB_HOST: "10.42.0.1"
  FLASK_APP: "tab_view"
  FLASK_DEBUG: "0"
  RATELIMIT_STORAGE_URI: "memory://"