apiVersion: v1
kind: ConfigMap
metadata:
  name: tabview-config
  namespace: prod
  labels:
    app: tabview
data:
  # For one machine
  # DB_HOST: "10.42.0.1"
  # For three machines
  DB_HOST: "192.168.8.160"
  FLASK_APP: "tab_view"
  FLASK_DEBUG: "0"
  RATELIMIT_STORAGE_URI: "memory://"